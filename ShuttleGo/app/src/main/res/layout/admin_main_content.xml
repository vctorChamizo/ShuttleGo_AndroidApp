<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="@drawable/bg_degraded">

    <LinearLayout

        style="@style/LinearProgress">

        <ProgressBar
            android:id="@+id/admin_main_content_progress"
            style="@style/ProgressBar" />
    </LinearLayout>

    <LinearLayout
        android:id="@+id/admin_main_content_linar1"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <android.support.design.widget.AppBarLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:theme="@style/AppTheme.AppBarOverlay">

            <android.support.v7.widget.Toolbar
                android:id="@+id/toolbar"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize"
                android:background="@color/colorPrimary"
                app:popupTheme="@style/AppTheme.PopupOverlay"
                app:title="@string/admin"/>
        </android.support.design.widget.AppBarLayout>

        <TextView
            android:id="@+id/driver_main_content_tittle"
            style="@style/TittleWelcome"
            android:textAlignment="center"
            android:layout_marginTop="20dp"
            android:layout_marginBottom="20dp"
            android:text="Crea un nuevo origen" />

        <LinearLayout
            android:id="@+id/driver_main_content_linear2"
            android:layout_marginTop="30dp"
            android:padding="20dp"
            style="@style/VerticalLinear" >

            <EditText
                android:id="@+id/admin_main_content_edittext"
                style="@style/DataLogin"
                android:layout_marginTop="10dp"
                android:hint="@string/origin"
                android:inputType="text"
                android:theme="@style/ThemeData" />
        </LinearLayout>


        <Button
            android:id="@+id/admin_main_content_button"
            android:text="@string/addOrigin"
            android:textColor="@color/colorPrimary"
            android:layout_marginTop="200dp"
            android:layout_marginHorizontal="20dp"
            style="@style/ButtonBigRound"/>
    </LinearLayout>
</LinearLayout>
